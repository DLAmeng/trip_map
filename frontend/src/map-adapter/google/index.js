/**
 * Google Maps adapter 第一版 stub。
 * Phase 3 只做 Leaflet;Phase 5 再按 app.js initGoogleMap L670-728 和
 * createGoogleMarker L1915-1970 补实现。接口先对齐,写法也会和 Leaflet 一致。
 */
export const createGoogleController = () => {
    throw new Error('GoogleMapsAdapter not implemented in Phase 3. Use Leaflet controller (or set config.mapProvider = "leaflet").');
};
